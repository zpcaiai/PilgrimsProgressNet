Drop hero .glb props here (e.g. wicket_gate.glb, doubting_castle.glb, celestial_gate.glb).
Then set the matching ModelProp src to /models/<file>.glb — the loader (Draco + KTX2) picks them up with no code change.
